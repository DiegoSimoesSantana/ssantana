// import { authMiddleware } from '@clerk/nextjs'

// ⚠️ MIDDLEWARE DESABILITADO TEMPORARIAMENTE PARA TESTES SEM CLERK
// Para habilitar autenticação:
// 1. Obtenha chaves em https://dashboard.clerk.com
// 2. Adicione no .env.local: CLERK_SECRET_KEY e NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY
// 3. Descomente o código abaixo

// export default authMiddleware({
//   publicRoutes: [
//     '/',
//     '/partners',
//     '/api/leads',
//     '/api/webhooks/(.*)',
//     '/sitemap.xml',
//     '/robots.txt',
//     '/opengraph-image',
//   ],
//   ignoredRoutes: ['/api/webhooks/(.*)'],
//   afterAuth(auth, req) {
//     if (req.nextUrl.pathname.startsWith('/api/webhooks')) return
//     if (!auth.userId && !auth.isPublicRoute) {
//       const signInUrl = new URL('/sign-in', req.url)
//       signInUrl.searchParams.set('redirect_url', req.url)
//       return Response.redirect(signInUrl)
//     }
//   },
// })

// export const config = {
//   matcher: ['/((?!.+\\.[\\w]+$|_next).*)', '/', '/(api|trpc)(.*)'],
// }
